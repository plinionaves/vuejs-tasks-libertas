{
    "scripts": {
        "start": "json-server db.json --watch --port 3000",
        "deploy": "now --public && now alias"
    },
    "devDependencies": {
        "json-server": "*"
    },
    "now": {
        "name": "vue-tasks-api-dev",
        "alias": "vue-tasks-api-dev"
    }
}
